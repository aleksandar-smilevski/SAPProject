@model IEnumerable<SAP.Models.OfflineSurvey>

@{
    ViewBag.Title = "Index";
}

<h3>Offline Surveys</h3>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Survey.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Survey.Date)
        </th>
        <th>
            Category
        </th>
        <th>
            Active
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Survey.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Survey.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Survey.Category1.Category1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Survey.Is_active)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
